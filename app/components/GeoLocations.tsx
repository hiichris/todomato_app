import React from "react";
import { View, Text } from "react-native";

export const GeoLocations = ({ styles }) => {
  return (
    <View style={styles.sectionContainer}>
      <Text style={styles.detailsSectionTitle}>Something to pin</Text>
    </View>
  );
};
